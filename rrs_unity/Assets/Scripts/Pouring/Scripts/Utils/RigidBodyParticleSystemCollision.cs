using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class RigidBodyParticleSystemCollision : MonoBehaviour {
    private void OnParticleCollision(GameObject other) {
        // print(this.gameObject.name + " collides with " + other.name);
    }
}